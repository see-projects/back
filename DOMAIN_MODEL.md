# See 커뮤니티 플랫폼 도메인 모델

## 🎯 도메인 개요

**See**는 **"사용자가 중심이 되는 콘텐츠 공유 커뮤니티"**를 비전으로 하는 플랫폼입니다. 회원들이 자유롭게 게시글을 작성하고, 댓글을 통해 소통하며, 유용한 콘텐츠를 공유하는 생태계를 구축합니다.

### 핵심 비즈니스 가치
- **콘텐츠 중심 생태계**: 사용자가 만드는 가치 있는 콘텐츠가 플랫폼의 중심
- **자유로운 소통**: 게시글과 댓글을 통한 활발한 커뮤니티 상호작용
- **개방형 플랫폼**: 누구나 참여하고 기여할 수 있는 민주적 공간
- **품질 있는 콘텐츠**: 사용자 스스로 만들어가는 건전한 커뮤니티 문화

## 🏗️ 아키텍처 설계 철학

### 헥사고날 아키텍처 (Ports & Adapters)
```
[외부 Actor] ←→ [Adapter] ←→ [Port] ←→ [Application] ←→ [Domain]
```

#### 의존성 방향
- **코드 의존성**: 항상 도메인(안쪽)을 향함
- **런타임 흐름**: 외부 → 어댑터 → 애플리케이션 → 도메인
- **기술 독립성**: 도메인은 외부 기술에 의존하지 않음

#### 계층별 책임

##### 도메인 계층 (Domain Layer)
- **순수한 비즈니스 로직**과 도메인 규칙
- **외부 의존성 없음** (프레임워크 독립적)
- **애그리거트**, **엔티티**, **값 객체**로 비즈니스 개념 표현

##### 애플리케이션 계층 (Application Layer)
- **유스케이스 구현** 및 도메인 객체들의 협력 조율
- **트랜잭션 경계** 관리
- **포트 인터페이스** 정의 (Provided/Required Interface)

##### 어댑터 계층 (Adapter Layer)
- **Primary Adapter**: 외부 요청을 애플리케이션으로 전달 (REST API, CLI)
- **Secondary Adapter**: 애플리케이션 요청을 외부 시스템으로 전달 (DB, 외부 API)

## 📋 주요 요구사항

### 기능적 요구사항

#### 1. 회원 관리 (Member Management)
- **회원가입**: 이메일, 닉네임, 비밀번호로 계정 생성
- **이메일 인증**: 가입 후 이메일 주소 확인으로 계정 활성화
- **로그인/로그아웃**: JWT 토큰 기반 사용자 인증
- **프로필 관리**: 닉네임, 소개, 프로필 이미지 수정
- **비밀번호 관리**: 비밀번호 변경 및 재설정
- **회원 상태 관리**: 활성, 비활성, 탈퇴 상태 전환

#### 2. 게시글 관리 (Post Management)
- **게시글 작성**: 제목, 내용, 카테고리를 설정하여 글 작성
- **임시저장**: 작성 중인 글을 임시로 보관
- **게시글 수정/삭제**: 작성자만 본인 글 관리 가능
- **게시글 조회**: 목록 조회, 상세 조회, 카테고리별 조회
- **게시글 검색**: 제목, 내용, 작성자 기준 검색
- **조회수 관리**: 게시글 열람 시 조회수 증가
- **첨부파일**: 이미지 및 문서 파일 업로드 지원

#### 3. 댓글 시스템 (Comment System) - 향후 구현
- **댓글 작성**: 게시글에 대한 의견 작성
- **댓글 수정/삭제**: 작성자만 본인 댓글 관리 가능
- **대댓글**: 댓글에 대한 답글 (최대 2단계 깊이)
- **댓글 신고**: 부적절한 댓글 신고 기능

#### 4. 인증 및 보안 (Authentication & Security)
- **JWT 기반 인증**: 무상태(stateless) 토큰 인증
- **권한 기반 접근 제어**: 사용자 역할별 기능 접근 권한
- **비밀번호 암호화**: BCrypt를 사용한 안전한 비밀번호 저장
- **세션 관리**: 토큰 만료 및 갱신 처리

### 비기능적 요구사항
- **성능**: 게시글 목록 조회 응답시간 < 200ms
- **보안**: 개인정보 보호, SQL Injection/XSS 방지
- **확장성**: 사용자 증가에 따른 수평 확장 가능
- **가용성**: 99.9% 서비스 가용성 목표
- **사용성**: 직관적이고 사용하기 쉬운 API 설계

## 🎨 도메인 모델 설계

### 1. 회원 애그리거트 (Member Aggregate)

#### 핵심 개념
**회원(Member)**은 See 커뮤니티에 참여하는 주체로, 게시글 작성과 댓글 활동의 주인입니다.

#### 구성 요소

##### Member (애그리거트 루트)
```java
@Entity
public class Member extends AbstractEntity {
    @NaturalId private Email email;        // 자연키 (고유 식별자)
    private String nickname;                // 커뮤니티 표시명 (고유)
    private String passwordHash;            // 암호화된 비밀번호
    private MemberStatus status;            // 회원 상태
    private MemberDetail detail;            // 회원 상세 정보 (1:1 관계)
}
```

##### MemberDetail (엔티티)
```java
@Entity
public class MemberDetail extends AbstractEntity {
    private Profile profile;                // 프로필 주소 (값 객체)
    private String introduction;            // 자기소개
    private LocalDateTime registeredAt;     // 등록 일시
    private LocalDateTime activatedAt;      // 활성화 일시
    private LocalDateTime lastLoginAt;      // 최종 로그인 일시
}
```

#### 값 객체 (Value Objects)

##### Email (공유 값 객체)
```java
public record Email(String address) {
    // 이메일 형식 검증
    // 최대 320자 제한 (RFC 5321 표준)
}
```

##### Profile (프로필 주소)
```java
public record Profile(String address) {
    // 프로필 주소 형식: 영소문자+숫자, 최대 15자
    // URL 표현: @{address}
}
```

#### 비즈니스 규칙

##### 회원 상태 전이
```
가입승인대기 (PENDING) → 활성 (ACTIVE) → 탈퇴 (DEACTIVATED)
                         ↑      ↓
                      관리자 재활성화
```

##### 핵심 제약사항
1. **이메일 중복 불가**: 동일한 이메일 주소로 여러 계정 생성 불가
2. **닉네임 중복 불가**: 커뮤니티 내 고유한 표시명 유지
3. **상태별 권한**: 활성 상태에서만 게시글 작성/수정 가능
4. **비밀번호 정책**: 최소 8자, 영문+숫자+특수문자 조합
5. **프로필 주소 규칙**: 영소문자+숫자만 허용, 최대 15자

#### 주요 유스케이스
- **회원가입**: 이메일 중복 검사 → 계정 생성 → 인증 이메일 발송
- **이메일 인증**: 인증 토큰 검증 → 계정 활성화
- **로그인**: 이메일/비밀번호 검증 → JWT 토큰 발급
- **프로필 수정**: 권한 확인 → 중복 검사 → 정보 변경
- **비밀번호 변경**: 기존 비밀번호 확인 → 새 비밀번호 설정

### 2. 게시글 애그리거트 (Post Aggregate)

#### 핵심 개념
**게시글(Post)**은 회원이 커뮤니티에 공유하는 콘텐츠 단위입니다.

#### 구성 요소

##### Post (애그리거트 루트)
```java
@Entity
public class Post extends AbstractAggregateRoot {
    private PostContent content;            // 게시글 내용 (값 객체)
    private Long authorId;                  // 작성자 ID (다른 애그리거트 참조)
    private PostCategory category;          // 카테고리
    private PostStatus status;              // 게시글 상태
    private PostMetaData metaData;          // 메타데이터 (값 객체)
}
```

##### PostContent (값 객체)
```java
public record PostContent(String title, String body) {
    // 제목: 1-100자, 필수
    // 본문: 1-50,000자, 필수
}
```

#### 열거형

##### PostStatus (게시글 상태)
```java
public enum PostStatus {
    DRAFT,      // 임시저장
    PUBLISHED,  // 게시됨
    HIDDEN,     // 숨김 (관리자 조치)
    DELETED     // 삭제됨
}
```

#### 비즈니스 규칙

##### 상태 전이 규칙
```
임시저장 (DRAFT) → 게시됨 (PUBLISHED) → 숨김 (HIDDEN)
                           ↓              ↓
                      삭제됨 (DELETED) ←────┘
```

##### 핵심 제약사항
1. **작성자 권한**: 본인이 작성한 게시글만 수정/삭제 가능
2. **필수 항목**: 제목과 본문은 필수 입력 사항
3. **상태 제한**: DRAFT에서 PUBLISHED로만 직접 전환 가능
4. **삭제 정책**: 댓글이 있는 게시글은 HIDDEN 처리 후 DELETED로 전환
5. **카테고리 분류**: 모든 게시글은 하나의 카테고리에 속해야 함

## 🔗 애그리거트 간 관계

### 참조 규칙
- **ID로만 참조**: 다른 애그리거트는 ID 값으로만 참조 (객체 참조 금지)
- **참조 무결성**: 애플리케이션 계층에서 참조 유효성 검증
- **독립적 트랜잭션**: 각 애그리거트는 별도 트랜잭션에서 처리

### 관계 정의
1. **회원 → 게시글**: 한 회원이 여러 게시글 작성 가능 (1:N)
2. **회원 → 댓글**: 한 회원이 여러 댓글 작성 가능 (1:N)
3. **게시글 → 댓글**: 한 게시글에 여러 댓글 작성 가능 (1:N)
4. **댓글 → 댓글**: 한 댓글에 여러 대댓글 작성 가능 (1:N)

## 📊 도메인 이벤트

### 회원 관련 이벤트
- **MemberRegistered**: 회원가입 완료 (이메일 인증 메일 발송 트리거)
- **MemberActivated**: 회원 활성화 (로그인 가능 상태 변경)
- **MemberProfileUpdated**: 프로필 변경 (변경 내역 로깅)

### 게시글 관련 이벤트
- **PostPublished**: 게시글 발행 (구독자 알림 트리거)
- **PostDeleted**: 게시글 삭제 (관련 댓글 정리 트리거)
- **PostViewIncremented**: 조회수 증가 (통계 데이터 업데이트)

## 🎯 비즈니스 제약사항 요약

### 데이터 제약사항
- **이메일**: RFC 5321 표준 형식, 중복 불가, 최대 320자
- **닉네임**: 중복 불가, 1-20자, 한글/영문/숫자만 허용
- **비밀번호**: 최소 8자, 영문+숫자+특수문자 조합 필수
- **게시글 제목**: 1-100자
- **게시글 본문**: 1-50,000자
- **댓글 내용**: 1-1,000자
- **프로필 주소**: 영소문자+숫자, 1-15자

### 상태 전이 제약사항
- **회원 상태**: PENDING → ACTIVE → DEACTIVATED (일방향)
- **게시글 상태**: DRAFT → PUBLISHED → HIDDEN/DELETED
- **댓글 상태**: ACTIVE → REPORTED/DELETED

### 권한 제약사항
- **수정/삭제**: 본인이 작성한 콘텐츠만 관리 가능
- **댓글 작성**: ACTIVE 상태의 회원만 가능
- **게시글 조회**: PUBLISHED 상태의 게시글만 조회 가능
- **관리자 권한**: 모든 콘텐츠 관리 가능 (HIDDEN 처리 등)

## 🚀 구현 우선순위

### Phase 1: 회원 기능 MVP (완료)
- ✅ 회원 도메인 모델 설계
- ✅ 회원가입/로그인 기능
- ✅ JWT 토큰 인증
- ✅ 프로필 관리 기능

### Phase 2: 게시글 핵심 기능 (진행중)
- ✅ 게시글 도메인 모델 설계
- 🚧 게시글 CRUD API 구현
- 🚧 게시글 목록 조회 (페이징)
- ⏳ 카테고리별 조회 기능

### Phase 3: 데이터 영속성 (예정)
- ⏳ JPA 엔티티 매핑 완성
- ⏳ Repository 구현체 작성
- ⏳ 데이터베이스 스키마 적용
- ⏳ 통합 테스트 작성

### Phase 4: 댓글 시스템 (예정)
- ⏳ 댓글 도메인 모델 설계
- ⏳ 댓글 CRUD 기능
- ⏳ 대댓글 계층 구조
- ⏳ 댓글 신고 기능

### Phase 5: 고급 기능 (예정)
- ⏳ 파일 업로드/다운로드
- ⏳ 게시글 검색 기능
- ⏳ 좋아요/북마크 기능
- ⏳ 실시간 알림 시스템

---

이 도메인 모델은 **헥사고날 아키텍처**와 **도메인 주도 설계** 원칙을 바탕으로 설계되었으며, **변경에 유연하고 테스트 가능한 시스템**을 구축하는 것을 목표로 합니다. 각 애그리거트는 독립적으로 발전할 수 있으며, 새로운 요구사항에 대응하여 점진적으로 확장 가능합니다.
